This directory holds data for the following tasks. 

* polling (in UAI 2014 paper).

* social survey. 





mat-repmat(mean(mat), size(mat,1), 1) # substract means.
mat./repmat(std(mat), size(mat, 1), 1) # normalize.

